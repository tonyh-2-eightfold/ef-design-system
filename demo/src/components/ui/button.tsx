/**
 * Canonical Button from the design system (Octuple DS, tokens only).
 */
import '../../../../src/components/Button/Button.css'

export {
  Button,
  buttonVariants,
  type ButtonProps,
  type ButtonVariant,
  type ButtonSize,
} from '@tonyh-2-eightfold/ef-design-system'
