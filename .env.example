# Figma API access token (for token sync scripts, design exports, etc.)
# Get one from Figma: Account → Settings → Personal access tokens
# Copy this file to .env and set your token. Never commit .env.
FIGMA_ACCESS_TOKEN=
