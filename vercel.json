{
  "buildCommand": "npm run build && cd demo && npm install && npm run build",
  "outputDirectory": "demo/dist",
  "framework": null,
  "installCommand": "npm install",
  "rewrites": [{ "source": "/((?!assets/|examples/).*)", "destination": "/index.html" }]
}
