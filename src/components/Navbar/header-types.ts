export type HeaderVariant = 'talent-acquisition' | 'career-hub'

/** Career Hub header sizes: profile (hero), parent (section), child (nested). */
export type CareerHubHeaderSize = 'profile' | 'parent' | 'child'
